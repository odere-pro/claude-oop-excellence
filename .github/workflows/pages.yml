# .github/workflows/pages.yml
# Publishes the standalone landing page in `site/` to GitHub Pages.
# The page is an indexable marketing/SEO surface, kept isolated from the
# Markdown docs so GitHub's Jekyll pipeline never processes `docs/*.md`.
#
# Hard invariants (mirrors the repo's other workflows):
# - Actions pinned by full commit SHA (not by tag/branch).
# - Least-privilege: `permissions: {}` at the workflow level; each job
#   widens only to what it needs (Pages deploy needs `pages: write` +
#   `id-token: write`).
# - Triggered by changes to the self-contained `site/` tree or this
#   workflow — plus manual dispatch for on-demand republish.
# - `site/` is author-only: it ships into the plugin cache but is never
#   loaded as a Claude Code component dir, so it costs nothing at runtime.
#
# Setup: the build job runs `configure-pages` with `enablement: true`, so the
# first successful run turns Pages on (Source = "GitHub Actions") with no manual
# Settings → Pages step required.

name: pages

on:
  push:
    branches: [main]
    paths:
      - "site/**"
      - ".github/workflows/pages.yml"
  workflow_dispatch:

permissions: {}

concurrency:
  group: pages
  cancel-in-progress: false

jobs:
  build:
    name: build site
    runs-on: ubuntu-24.04
    permissions:
      contents: read
      # `enablement: true` below turns Pages on for the repo on first run, so
      # the build job needs `pages: write` (it would otherwise fail with
      # "Get Pages site failed … verify that the repository has Pages enabled").
      pages: write
      id-token: write
    steps:
      - name: Checkout
        uses: actions/checkout@df4cb1c069e1874edd31b4311f1884172cec0e10 # v6.0.3
        with:
          persist-credentials: false

      - name: Configure Pages
        uses: actions/configure-pages@45bfe0192ca1faeb007ade9deae92b16b8254a0d # v6.0.0
        with:
          # Auto-enable Pages (Source = GitHub Actions) instead of requiring a
          # manual one-time Settings → Pages click.
          enablement: true

      - name: Upload artifact
        uses: actions/upload-pages-artifact@fc324d3547104276b827a68afc52ff2a11cc49c9 # v5.0.0
        with:
          path: site

  deploy:
    name: deploy to Pages
    needs: build
    runs-on: ubuntu-24.04
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    permissions:
      pages: write
      id-token: write
    steps:
      - name: Deploy
        id: deployment
        uses: actions/deploy-pages@d6db90164ac5ed86f2b6aed7e0febac5b3c0c03e # v4
